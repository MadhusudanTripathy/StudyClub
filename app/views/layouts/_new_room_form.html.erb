<%= form_with(model: @room, remote: true, class: "d-flex" ) do |f| %>
  <%= f.text_field :name, class:"chat-room-input-field",placeholder:"Add Chat Room", autocomplete: 'off' %>
  <%= f.submit 'Add Room' ,class:"chat-room-add-btn", data: { disable_with: false } %>
<% end %>

