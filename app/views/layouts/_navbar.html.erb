<nav class="nav-bar flex-alignments">
  <div class="nav-logo text-alignments">
    <li><i class="fa-solid fa-cube text-color"></i>
      <%=link_to :studyclub, questions_path, class:"text-color"%>
    </div>
    <div class="search-container flex-alignments content-justify-center">
      <%=form_tag(search_path,:method=>"get") do%>
        <%= search_field_tag :q, params[:q], placeholder:"Search Your Question", class:"text-alignments default-font-size border-style"%>
        <%# <%= button_tag 'search', :name=>nil %>
      <% end %>
    </div>
    <% if user_signed_in? %>
      <div class="nav-lists">
        <ul class="nav-items flex-alignments content-justify-center">
          <div class="nav-profile-img">
            <% if get_dashboard_instance(current_user.id) %>
              <% if get_dashboard_instance(current_user.id).image.attached? %>
                <%= image_tag get_dashboard_instance(current_user.id).image,class:"radius" %>
              <% else %>
                <%= image_tag 'https://aui.atlassian.com/aui/8.8/docs/images/avatar-person.svg', class:'radius' %>
              <% end%>
            <% else %>
              <%= image_tag 'https://aui.atlassian.com/aui/8.8/docs/images/avatar-person.svg', class:'radius' %>
            <% end %>
          </div>
          <li class="name"><%= current_user.full_name %></li>
          <div class="tooltip">
            <%= button_to '',destroy_user_session_path, method: :delete, class: "fa fa-sign-out" %>
            <span class="tooltiptext">Log out</span>
          </div>
        </ul>
      </div>
    <% else %>
      <div class="nav-lists">
        <ul class="nav-items flex-alignments content-justify-center">
          <!-- <li><a href="#">Home</a></li> -->
          <!-- <li><a href="#">Blogs</a></li> -->
          <!-- <li><a href="#">About</a></li>
                    <li><a href="#">Contact</a></li> -->
          <li class="default-font-size">
            <%= link_to 'Sign In', new_user_session_path,class:'btn btn-active text-alignments default-font-size border-style'  %>
          </li>
          <li class="default-font-size">
            <%= link_to 'Sign Up',new_user_registration_path,class:'btn btn-inactive text-alignments default-font-size border-style'%>
          </li>
        </ul>
      </div>
    <% end %>
  </nav>
