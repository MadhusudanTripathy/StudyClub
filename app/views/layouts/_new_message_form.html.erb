 <%= form_with(model: [@single_room ,@message], remote: true ) do |f| %>
  <%= f.text_field :content,placeholder:'Write Message' , id: 'chat-text', autocomplete: 'off',cols:'30',rows:'1' %>
  <%#= f.submit data: { disable_with: false }  %>
<% end %>
