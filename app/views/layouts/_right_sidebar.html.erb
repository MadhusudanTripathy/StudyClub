<div class="right-container">
  <div class="section-right">
    <div class="question-answer">
      <div class="score-cards">
        <div class="card question-asked-card">
          <h5>Asked <br>
            Questions</h5>
          <big><%= current_user.questions.length.to_s.rjust(2, '0') %></big>
        </div>
        <div class="card answer-asked-card">
          <h5>Answered <br>
            Questions</h5>
          <big><%= current_user.answers.length.to_s.rjust(2, '0') %></big>
        </div>
      </div>
      <div class="trending-cards">
        <h3>Trending Topics Today</h3>
        <%get_trend_hash[0..2].each do |arr|%>
          <div class="trending-topics">
            <h5>
              <%= link_to arr[0],category_path(arr[0]) %>
            </h5>
            <span class="total-answers"><%=arr[1]%> Questions Posted</span>
          </div>
        <%end%>
      </div>
    </div>
  </div>
</div>
<style>

:root {
    --background-color: #E0F1FF;
    --text-heading: #303841;
    --text-details: #cccccc;
    --font-size: 16px;
}
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    list-style: none;
    font-family: Arial, Helvetica, sans-serif;
}
.right-container{
    display: flex;
    flex-direction: column;
    min-height: 89vh;
    flex:1;  
}
.section-right{
    display: flex;
    justify-content: space-evenly;
    flex-direction: column;
    /* flex: 1; */
}
.question-answer{
    display:flex;
    flex-direction: column;
    justify-content: space-evenly;
    min-height: 89vh;
}
.question-answer .score-cards{
    display: flex;
    justify-content: space-between;
    column-gap:10px;
    margin: 1rem;
}
.card{
    display: flex;
    flex-direction: column;
    padding:12px;
    outline: none;
    box-shadow: 0px 0px 6px rgba(0, 0, 0, 0.3);
    flex: 1;
}
.card h5{
    text-align: center;
    font-size: 1.1rem;
    color: var(--text-heading);
}
.card big{
    text-align: center;
    font-size:3.5rem ;
    color: var(--text-details);
}

.question-answer .trending-cards{
    margin: 1rem;
    display: flex;
    /* justify-content: center; */
    flex-direction: column;
    padding: 12px ;
    outline: none;
    box-shadow: 0px 0px 6px rgba(0, 0, 0, 0.3);
    flex:1;
}
.trending-topics{
    margin-top: 1rem;
}
.trending-cards h3{
    color: var(--text-heading);
}

.trending-topics h5 a{
    color:#212c29;
    text-decoration: none;
    font-size: var(--font-size);
}
.trending-topics h4{
    margin:4px auto;
}
.trending-topics span.total-answers{
    font-size: 12px;
    font-weight: 600;
    color: #212c29;
}
@media only screen and (max-width: 950px) {

    .right-container{
        flex-wrap: wrap;
    }
    .trending-cards h3{
        text-align: center;
    }

}

::-webkit-scrollbar {
    display: none;
}
.asking-section{
    -ms-overflow-style: none; /* for Internet Explorer, Edge */
    scrollbar-width: none; /* for Firefox */
    overflow-y: scroll;
}

</style>